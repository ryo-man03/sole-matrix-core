import { NextResponse } from "next/server";import { authorizeInternalJob } from "../../../../../src/application/daily-picks/internalJobAuth";import { buildDailyBatch } from "../../../../../src/application/daily-picks/buildDailyBatch";import { validateMutationRequest } from "../../../../../src/application/http/requestSecurity";
export async function POST(r:Request){const g=validateMutationRequest(r,{key:"daily-job",limit:10});if(!g.ok)return NextResponse.json({ok:false},{status:g.status});if(!authorizeInternalJob(r))return NextResponse.json({ok:false,error:{code:"UNAUTHORIZED_JOB"}},{status:401});try{const b=await r.json();if(typeof b!=="object"||b===null||Array.isArray(b)||Object.keys(b).some((k)=>!["userId","targetDate"].includes(k)))throw new Error("INVALID_JOB");const v=b as Record<string,unknown>;if(typeof v.userId!=="string"||!/^[0-9a-f-]{36}$/i.test(v.userId)||typeof v.targetDate!=="string")throw new Error("INVALID_JOB");return NextResponse.json({ok:true,data:await buildDailyBatch(v.userId,v.targetDate)})}catch(e){return NextResponse.json({ok:false,error:{code:e instanceof Error?e.message:"JOB_FAILED"}},{status:400})}}
