import { NextResponse } from "next/server";import { parseSize } from "../../../../src/domain/collection/collection";import { createOwnedRow,listOwnedRows } from "../../../../src/infrastructure/repositories/personalizationRepository";import { failure,guard,privateUser,unauthenticated } from "../../../../src/application/personalization/routeHelpers";import { readBoundedJsonBody } from "../../../../src/application/http/requestSecurity";
export async function GET(){const u=await privateUser();if(!u)return unauthenticated();try{return NextResponse.json({ok:true,data:{sizes:await listOwnedRows("user_sizes",u.id)}})}catch(e){return failure(e)}}
export async function POST(r:Request){const g=guard(r,"size-create");if(!g.ok)return NextResponse.json({ok:false},{status:g.status});const u=await privateUser();if(!u)return unauthenticated();try{return NextResponse.json({ok:true,data:{size:await createOwnedRow("user_sizes",u.id,parseSize(await readBoundedJsonBody(r)))}},{status:201})}catch(e){return failure(e)}}
