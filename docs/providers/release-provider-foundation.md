# Release provider foundation

Only `fixture` and `manual_seed` are active. Both prohibit automatic collection, image reuse, and article excerpt reuse. The application does not access Nike, Sneaker Wars, SNKRDUNK, RSS guesses, marketplace listings, or public HTML for release data. Release evidence and market listings are different schemas.
