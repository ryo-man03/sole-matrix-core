# Today

`/today` is a read-only view of a previously generated private batch. No batch is a normal empty state. A prior batch is visibly stale and includes its generation time. Fewer than eight suitable candidates is valid; rumors and owned duplicates never fill space. Technical score and canonical details are collapsed by default. Feedback writes are authenticated and RLS-protected.
