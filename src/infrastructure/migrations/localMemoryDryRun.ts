export type LocalMemoryDryRun={trust:"untrusted_user_data";profile:{userId:string;displayName:string}|null;feedbackCount:number;warnings:string[];sourceDeleted:false};
export function inspectLegacyMemory(content:string):LocalMemoryDryRun{const warnings:string[]=[];if(content.length>1_000_000)warnings.push("source_too_large");if(/(?:ignore previous|system prompt|developer message|<script)/iu.test(content))warnings.push("instruction_like_content_ignored");const userId=jsonLine(content,"userId"),displayName=jsonLine(content,"displayName");if(!userId||!displayName)warnings.push("profile_missing");return{trust:"untrusted_user_data",profile:userId&&displayName?{userId,displayName}:null,feedbackCount:[...content.matchAll(/^### Feedback /gm)].length,warnings,sourceDeleted:false};}
function jsonLine(content:string,key:string){const m=content.match(new RegExp(`^- ${key}: (.*)$`,"m"));if(!m?.[1])return null;try{const v=JSON.parse(m[1]);return typeof v==="string"?v:null}catch{return null}}
